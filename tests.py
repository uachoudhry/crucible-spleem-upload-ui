# === 1) Test that you can upload a session of files
# === 2) Add a file to the folder and test that only the new file gets uploaded
# === 3) Change the config to IS_SESSION = False and test that you can upload a single file
# === 4) Test that you can select multiple files and upload them - only new files in the project get uploaded
